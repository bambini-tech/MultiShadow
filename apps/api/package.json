{
  "name": "@multishadow/api",
  "version": "0.1.0",
  "private": true,
  "description": "Houdini Swap proxy. Runs as a standalone HTTP server (Railway/containers) or as Vercel/Netlify serverless functions. Injects the API key server-side; the key never reaches the browser.",
  "type": "module",
  "scripts": {
    "start": "tsx server.ts",
    "dev": "tsx watch server.ts",
    "dev:vercel": "vercel dev",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@multishadow/core": "workspace:*",
    "tsx": "^4.19.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "@vercel/node": "^3.2.24",
    "typescript": "^5.7.2"
  }
}
